int len = 0;
    // 計算字串長度
    while (src[len] != '\0') {
        len++;
    }

    // 將字串反轉
    for (int i = 0; i < len; i++) {
        dest[i] = src[len - 1 - i];
    }

    // 字串結尾加上 '\0'
    dest[len] = '\0';

    return dest;
